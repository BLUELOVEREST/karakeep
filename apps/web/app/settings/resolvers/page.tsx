import ResolverSettings from "@/components/settings/ResolverSettings";

export const metadata = {
  title: "Resolver Settings | Karakeep",
};

export default function ResolverSettingsPage() {
  return <ResolverSettings />;
}
